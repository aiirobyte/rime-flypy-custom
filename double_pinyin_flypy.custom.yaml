patch:
  key_binder/bindings:
    # Lua 配置: 以词定字（上屏当前词句的第一个或最后一个字），和中括号翻页有冲突
    - select_first_character: "bracketleft"  # 左中括号 [
    - select_last_character: "bracketright"  # 右中括号 ]
  __include: symbols:/patch #将 symbols.yaml 中配置覆盖到方案配置中